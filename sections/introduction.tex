\section{Introduction}

% TODO: Below is mainly ai generated I think, make a list of what should be said in the intro and reformulate this

The interaction between users and AI is currently limited as adaptive large language models are constrained by static interfaces that force users into either unstructured chat windows or rigid, pre-built applications. While recent innovations allow LLMs to generate entire webpages, these interfaces disrupt workflow by completely replacing the user's current context. Real-world tasks require assistance that augments, rather than replaces, the active application. Users need intermediate UI elements like sliders or tables to allow them to explore variables and make decisions without constant, open-ended re-prompting.

Research in computational notebooks has already validated the utility of ``ephemeral UIs'' as effective scaffolds for generative tasks. Studies show that these temporary interfaces significantly aid users in understanding generative outputs by offering visual representations of code variables. Furthermore, they reduce the cognitive load of prompt engineering by allowing users to customize parameters through direct manipulation rather than complex text description, fostering a ``learning by doing'' approach. However, a generalized framework for applying these benefits to broader productivity tools remains unexplored.

This proposal introduces \textbf{Ephemeral Interfaces}: temporary, task-scoped UI components generated on-demand within existing applications that persist only for the duration of a specific task. To operationalize and evaluate this concept, I will follow a Design Science Research methodology to develop a conceptual design framework and a custom React library (\texttt{react-ephemeral-experimental}) as a research instrument. This artifact will support a user study evaluating how transient, generative UIs impact task efficiency and user mental models compared to static alternatives.

How can ephemeral, task-scoped generative UI components be designed to augment existing applications without disrupting user workflow?

\subsection{Literature Review}
Generative AI excels at tasks that requires it to generat content. Current research shows that GenAI does a great job in creating UI that users genuinely enjoy instead of the traditional static content it can generate if you are using a general chat interface \cite{Leviathan2025GenerativeUI}. However, it this research only addresses experiences that are completely generated with UI. In reality, users when they use applications on a day to day experience want to have it predictable. By doing so they will understand how to use the tools they have access to. There is no exploration on seeing how it would be if users were to have a blend of generative ui and traditional static ui.

LLMs have provided immense value to the technical community especially when it comes to generating code. However, most developers had hard time understanding the code generated by the LLM. So, some research used ephemereal UI as an intermediate step between the developers putting in the prompt and code generation to help them understand and explore the AI-generated code \cite{Cheng2024Biscuit}. It shows an example of a great use case for ephemereal UI where the goal of the exploration is stochastic so generating a UI for every exploration can provide an efficient way of leveraging AI with better observability. However, this research does not explore a wider range of use cases for ephemereal UI. It only explored it with a single intention for helping developers explore and understand AI-generated code. No advancements were done if the intention was focused on how to improve the customer checkout experience for an ecommerce website for users with unique tastes.

Furthermore, traditional ui is designed for permence which can create a cognitive overload in ubiquitous environments. Research for the efficacy for ephemereal interfaces has been done in regards to the physical world \cite{Doring2013EphemeralUI}. For example they used 3 designs spaces with the materials, interaction, and ephemerality. For example they used soap bubble interface where the bubbles control the light. It provides the theoretical foundation for the reason of why ephemerality could be a good use case of LLMs as it can help address the cognitive overload. Also this paper proves that material semmantics carry meaningul connotations beyond function could be carried to ephemereal generative uis. However, this research is limited to only research for the physical world and not generative UI.

transience is not talked about yet dk lol

However, a clear gap remains. Research has only been done on full-page generative UI, while others restrict their intentions to only helping developers understand ai generated code on computational notebooks. Ephemereal UI is proven to be a valid use case for improving UI / UX. This research builds upon the validation of ephemeral workflow and generalises it to make task-scoped generative patterns for everyday productivity applications.

However, a clear gap remains. some research focuses on full-page replacements \cite{Leviathan2025GenerativeUI}, whil others restrict their findings to computational notebooks \cite{Cheng2024Biscuit}. This research builds upon validation of the ``ephemeral'' workflow and generalizes it to create task-scoped generative patterns for everyday productivity applications.
